export default {
  /**
   * The play cache size.
   * @type Number
   */
  buffer: 1,

  /**
   * Set transition size. If not enable, set to 1.
   * @type Number
   */
  transition: 0,

  /**
   * Set the layer maximum opacity.
   * @type Number
   */
  opacity: 0.6,
}
