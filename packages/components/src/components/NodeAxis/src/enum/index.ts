export enum Extensible {NONE = 0, HEAD = 1, TAIL = 2, ALL = 3}
export enum Direction {FORWARD = 1, BACKWARD = -1}
export enum TimeModes { DAILY = 'daily', INTERVAL = 'interval', STATISTIC = 'statistic' }
